<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lplb.sys.modular.emp.mapper.SysEmpMapper">

    <select id="getUserOrgNames" resultType="java.lang.String">
        SELECT org_name FROM sys_emp WHERE id=#{userId}
    </select>
    <select id="getOrgUsers" resultType="com.lplb.sys.modular.user.entity.SysUser">
        SELECT su.id FROM sys_user su
        INNER JOIN sys_emp se ON su.id=se.id AND se.org_id=#{orgId}
        WHERE su.status!=2
    </select>
</mapper>
